import React, { FC } from 'react';
// Define the props interface (if there are any)
interface TopProps {
  // Add props here if needed
}

const Top: FC<TopProps> = () => {
  return <div>Top</div>;
};

export default Top;
